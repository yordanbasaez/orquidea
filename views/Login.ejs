<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar sesión</title>

    <!-- Enlace a Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Enlace a Animate.css (para animaciones UI) -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">

    <!-- Enlace a Font Awesome (para iconos) -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

    <style>
        body {
            background-image: url('/planta_verder.jpg'); 
            background-size: cover; 
            background-position: center; 
            background-attachment: fixed;
            background-color: #f8f9fa; 
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            max-width: 400px;
        }

        .card {
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2); /* Sombra sutil */
            background-color: rgba(255, 255, 255, 0.3); /* Fondo blanco con opacidad */
            backdrop-filter: blur(8px); /* Desenfoque del fondo */
            opacity: 0;
            animation: fadeIn 1s forwards;
        }

        .card-header {
            background-color: rgba(40, 167, 69, .9); /* Verde semi-transparente */
            color: white;
            text-align: center;
            font-size: 1.5rem;
            animation: bounceInDown 1s;
        }

        .card-body {
            padding: 2rem;
        }

        .form-label {
            font-weight: 500;
        }

        .form-control {
            border-radius: 5px;
            padding: 0.75rem;
        }

        .btn-green {
            background-color: #28a745;
            color: white;
            border-radius: 5px;
            padding: 0.75rem;
            transition: background-color 0.3s;
        }

        .btn-green:hover {
            background-color: #218838;
        }

        .link {
            
            color: #93d4a2;
        }

        .link:hover {
            color: #315c3b;
            text-decoration: none;
            color: white;
        }

        .alert {
            border-radius: 5px;
            animation: fadeInUp 1s;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        @keyframes fadeInUp {
            from {
                transform: translateY(20px);
                opacity: 0;
            }

            to {
                transform: translateY(0);
                opacity: 1;
            }
        }
    </style>
</head>

<body>

    <div class="container">
        <div class="card animate__animated animate__fadeInUp">
            <div class="card-header animate__animated animate__bounceInDown">
                Iniciar sesión
            </div>
            <div class="card-body">
                <% if (message) { %>
                    <div class="alert alert-danger animate__animated animate__fadeInUp">
                        <%= message %>
                    </div>
                <% } %>

                <form action="/login" method="post">
                   <div class="mb-3">
                        <div class="input-group">
                            <span class="input-group-text"><i class="fas fa-user"></i></span>
                            <input type="text" id="username" name="username" class="form-control" placeholder="Nombre de usuario" required>
                        </div>
                    </div>

                    <div class="mb-3">
                        <div class="input-group">
                            <span class="input-group-text"><i class="fas fa-lock"></i></span>
                            <input type="password" id="password" name="password" class="form-control" placeholder="Contraseña" required>
                        </div>
                    </div>

                    <button type="submit" class="btn btn-green w-100 animate__animated animate__fadeIn animate__delay-1s">Iniciar sesión</button>
                </form>

                <p class="text-center mt-3">¿No tienes una cuenta? <a href="/register" class="link">Regístrate aquí</a></p>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>

</body>

</html>

